# SafeRun cURL Examples

Use the scripts to interact with SafeRun API directly.

- `archive-repo.sh` — dry-run archive repo
- `delete-branch.sh` — dry-run delete branch
- `approve-change.sh` — approve change via change_id
